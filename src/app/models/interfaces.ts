export interface BubbleTea {
  id: number;
  name: string;
  temperature: number;
  price: number;
  active: boolean;
}

export interface User {
    id: number;
    name: string;
    email: string;
    password: string;
}