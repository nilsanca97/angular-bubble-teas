export interface BubbleTea {
  id: number;
  name: string;
  temperature: number;
  precio: number;
  active: boolean;
}

export interface User {
    id: number;
    name: string;
    email: string;
    password: string;
}